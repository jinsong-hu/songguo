# Binaries
/songguo
/songguo-*

# SQLite database and WAL/SHM sidecar files
*.db
*.db-*

# Real config (only the example is committed)
/config.yaml

# Web build artifacts (dist is committed so the binary builds without Node)
web/node_modules/
web/*.tsbuildinfo

# Misc
.DS_Store
*.log
/tmp/
